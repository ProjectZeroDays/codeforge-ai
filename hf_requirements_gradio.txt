# =============================================================================
# CodeForge AI - Gradio-only Requirements
# =============================================================================
# Minimal dependencies for Gradio interface on HF Spaces free tier
# =============================================================================

# Gradio UI
gradio==4.16.0

# FastAPI Backend (minimal)
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Database (SQLite only)
aiosqlite==0.19.0
sqlalchemy[asyncio]==2.0.25
greenlet==3.0.3

# AI Integration
httpx==0.26.0

# GitHub Integration
PyGithub==2.1.1

# Document Parsing (minimal)
python-docx==1.1.0
PyPDF2==3.0.1

# YAML Processing
PyYAML==6.0.1

# Encryption
cryptography==42.0.2

# Utilities
pydantic==2.6.0
aiofiles==23.2.1

# HuggingFace
huggingface-hub==0.20.3
